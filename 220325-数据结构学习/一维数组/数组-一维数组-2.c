#include <stdio.h>
#define N 50

void main(){
    char str[N];
    int i,a=0,b=0,c=0;
    printf("Please input a string\n");
    // scanf("%s",&str);    // 教材这里写的是gets(str),不知道这个可不可以用
    gets(str);
    for(i=0;str[i]!='\0';i++){  // '\0'是什么意思

    }
}